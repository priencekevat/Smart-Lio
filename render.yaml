services:
  - type: web
    name: smart-lio
    env: python
    buildCommand: pip install --upgrade pip && pip install fastapi uvicorn && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT